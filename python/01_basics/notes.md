# Основы - Конспект

## Конспект по теме: "Ввод - вывод данных, команды print() и input(), переменные"

### Основные понятия:

#### **`print()`**

Для вывода данных на экран используется команда **`print()`**;

**`print()`** имеет свои **аргументы** - конкретные значения, которые передаются функции при её вызове; 

Команда **`print()`** позволяет указывать несколько аргументов, в таком случае их **надо отделять запятыми**;

#### **`input()`**

Программы могут работать с данными, которые станут известны только во время выполнения программы. Другими словами, программы могут **считывать данные**, а затем их **использовать**.

Для считывания данных в языке Python используется команда **`input()`**
  - команда **`input()`** всегда пишется с круглыми скобками.
  - Она работает так: когда программа доходит до места, где есть `input()`, она ждет, пока пользователь введёт текст с клавиатуры(ввод завершается нажатием клавиши **Enter**). Введенная строка подставляется на место **`input()`**.
  - Таким образом, команда **`input()`** получает от пользователя (нас) какие-то данные и в место вызова подставляет **строковое значение**.

#### **Переменные**

Команда **`input()`** обозначает: "подожди, пока пользователь введет что-нибудь с клавиатуры, и запомни то, что он ввел". 

Просто так просить "запомнить" довольно бессмысленно: нам ведь потом надо будет как-то сказать компьютеру, чтобы он вспомнил то, что запомнил! 

- Для этого мы используем **переменные** и пишем следующий код:

    ```python
    name = input() # создаём переменную с именем name
    print('Вы ввели имя:', name)
    ```

Данный код означает: "Сохрани то, что ввел пользователь, в памяти, и дальше это место в памяти мы будем называть именем name". 

Соответственно, команда **`print(name)`** означает: "Посмотри, что лежит в памяти под именем name, и выведи это на экран".

Вот такие "места в памяти" и называются переменными. **Любая переменная имеет имя и значение**.

##### Имя переменной:

  1. В имени переменной рекомендуется использовать только **латинские буквы a-z, A-Z, цифры и символ нижнего подчеркивания (_)**
  2. Имя переменной **не может начинаться с цифры**
  3. Имя переменной **не может содержать пробелы**
  4. Имя переменной по возможности **должно отражать ее назначение**
  5. Python – регистрочувствительный язык. Переменные name и Name – две **совершенно разные** переменные.
  6. Для именования переменных принято использовать стиль lower_case_with_underscores(слова из маленьких букв с подчеркиваниями).

     Например, следующие имена для переменных хорошие:
     
     ```python
     server_response
     is_password_good
     username
     red_dead_redemption_2
     ```
В качестве названия переменных запрещено использовать **ключевые(зарезервированные) слова**. 
К ключевым словам в языке Python относятся:

1. False;
2. True;
3. None;
4. and;
5. with;
6. as;
7. assert;
8. break;
9. class;
10. continue;
11. def;
12. del;
13. elif;
14. else;
15. except;
16. finally;
17. try;
18. for;
19. from;
20. global;
21. if;
22. import;
23. in;
24. is;
25. lambda;
26. nonlocal;
27. not;
28. or;
29. pass;
30. raise;
31. return;
32. while;
33. yield.

##### Значения переменной:

**Символ  `=`** в языках программирования выступает в роли **оператора присваивания**. Он выполняет задачу связывания значения, расположенного справа от него, с именем переменной, указанной слева. Таким образом, переменная слева "принимает" или "хранит" значение, представленное выражением справа от знака **`=`**.
  1. **Значение переменной** - это **сохранённая в ней информация**: текст, числа, булевы значения и т.д. и т.п.
  2. **Название переменной** всегда должно быть **слева от знака равенства**.
  3. Значения переменной, естественно, **можно менять** (переприсваивать).
  4. Оператор присваивания сообщает **переменной** то или иное значение **независимо от того, была ли эта переменная введена раньше**.
  5. Можно менять **значение переменной**, записав еще один **оператор присваивания**. Если имеется переменная, мы можем делать с ней **все что угодно** — например, присвоить другой переменной или изменить её значение.
     
     
### Важные моменты:

#### Для команды `print()`:

Внутри круглых скобок команды **`print()`** пишем, что хотим вывести на экран. Если это текст, то обязательно указываем его  внутри кавычек. 
  - Кавычки могут быть **одинарными** (') или **двойными** ("), до и после текста мы ставим **только одинаковые** кавычки.
  - Python не делает различий между одинарными и двойными кавычками: они работают одинаково для определения строк.

В качестве разделителя при выводе данных между аргументами команды используется **символ пробела**.
  - По умолчанию команда **`print()`** добавляет **ровно один пробел** между всеми своими **аргументами**.

При написании кода между аргументами команды **`print()`** после запятой ставится один символ пробел. Этот символ никак не влияет на вывод данных, но делает код гораздо более читабельным.

Команда **`print()`** записывается только маленькими буквами, другое написание недопустимо, так как в Python **строчные и заглавные буквы различны**.

Каждая последующая команда **`print()`** выводит указанный текст с **новой строки**.

Команда **`print()`** с пустым списком аргументов просто вставляет **новую пустую строку**.

В Python можно использовать как **одинарные**, так и **двойные** кавычки для обрамления текста потому, что часто кавычки являются частью текста.
  Чтобы Python мог правильно распознать такой текст, пользуемся разными:
  - если в тексте нужны одинарные кавычки, то для обрамления такого текста используем **двойные** и **наоборот**.
  - мы не можем использовать одинарные кавычки в строке, если саму строку обрамляем одинарными кавычками. С двойными кавычками ситуация аналогичная.

#### Для команды `input()`:

Очень часто перед считыванием данных мы печатаем некоторый текст, чтобы пользователь, который вводит эти данные, понимал, что именно от него требуется. 

- Например, в программе:

    ```python
    print('Как тебя зовут?')
    name = input()
    ```

Сначала выведется текст "Как тебя зовут?", а уже потом считаются данные.  

Поскольку это достаточно распространенный сценарий, то в языке Python можно выводить текст, передавая его в качестве аргумента в команду **`input()`**. 

- Предыдущий код можно переписать так:

    ```python
    # сначала тут печатается строка 'Как тебя зовут', а потом принимается на вход имя
    name = input('Как тебя зовут?')
    ```
То есть команда **`input()`** при наличии **аргументов внутри нее** отрабатывает одновременно как вывод текста, а потом ввод текста (именно в этом порядке).

Интерпретатор ждет, что пользователь что-то введет с клавиатуры **ровно столько раз, сколько команд `input()`** встречается в программе. Каждый **`input()`** завершается нажатием **Enter** на клавиатуре.

#### Для переменных:

Переменные можно вводить в **любой момент (не только в самом начале программы)**.

При попытке считывания больших данных, чем поступают на вход, возникает ошибка **`EOFError`("End-of-File Error") - "ошибка конца файла"**.

**Названия переменных ничего не говорят интерпретатору о значениях в этих переменных**, и даже в очень хорошо названной переменной не появится нужное значение, если мы сами его туда не запишем.

### Примеры кода:
1. Вывод нескольких аргументов:
   
    ```python
    print('A', 'B', 'C', 'D', 'и так далее')
    ```
    Выводит:

    ```python
    A B C D и так далее
    ```
3. Вывод текста с новой строки последующей командой **`print()`**:
   
   ```python
   print('A', 'B', 'C', 'D', 'и так далее')
   print('E', 'F', 'G', 'и так далее')
   ```
   Выводит:
   
   ```python
   A B C D и так далее
   E F G и так далее
   ```
4. Команда **`print()`** с пустым  списком аргументов:
   
   ```python
   print('A', 'B', 'C', 'D', 'и так далее')
   print()
   print('E', 'F', 'G', 'и так далее')
   ```
    Выводит:
   
    ```python
    A B C D и так далее
 
    E F G и так далее
    ```
5. Использование кавычек для обрамления текста:
   
   ```python
   print('В тексте есть "двойные" кавычки')
   print("В тексте есть 'одинарные' кавычки")
   ```
    Выводит:
   
    ```python
    В тексте есть "двойные" кавычки
    В тексте есть 'одинарные' кавычки
    ```
6. К чему приводит использование одних и тех же ковычек при написании аргумента **`print()`**:

   ```python
   print('12 ' 34 ')
   ```

   Выводит:

   ```python
   SyntaxError: unterminated string literal (detected at line 1)
   ```
7. Пример использования команды **`input()`**:
   
   ```python
   print('Как тебя зовут?')     # вывод текста
   print('Привет,', input())    # ввод текста и вывод текста
   ```
   Сначала программа распечатает текст на экран 'Как тебя зовут?'.
   Далее программа будет ждать от пользователя ввода данных. Ввод данных реализуется с помощью команды **`input()`**.
   И в конце произойдет вывод текста вместе с введенными от пользователя данными.

8. Пример переприсваивания значения переменной:
   
   ```python
   print('Как тебя зовут?')     # вывод текста
   name = input() # создание переменной для ввода данных от пользователя
   print('Привет,', name) # вывод текста вместе с данными от пользователя

   name = 'Trofim' # присваивание переменной нового значения
   print('Привет,', name) # вывод текста вместе с переприсвоенным значением
   ```
9. Пример того, что будет, если ввести название переменной **справа** от знака равенства:
    
    ```python
    'Trofim' = name 
    ```

   Выводит:

    ```python
    SyntaxError: cannot assign to literal here. Maybe you meant '==' instead of '='?
    ```
10. Пример работы сразу с несколькими строками:
    
    ```python
    first = input()
    second = input()
    print('I am', first, 'and', second)
    ```

    Если на вход программе будут поданы строки:
    
    ```python
    cool
    I know it
    ```

    
    То программа выведет:

     ```python
     I am cool and I know it
     ```
    
## Конспект по теме: "Необязательные параметры команды `print()` `sep`, `end`, стандарт PEP8"

### Основные понятия:

По умолчанию команда **`print()`** принимает **несколько аргументов**, выводит их через **один пробел**, после чего ставит **перевод/перенос строки**. 

Это поведение можно изменить, используя необязательные именованные параметры **`sep`** и **`end`**.

#### Параметр **`sep`**

Параметр **`sep`(separator - разделитель)** - это **параметр команды `print()`**, который отвечает за разделение аргументов при выводе. 

Также необязательный параметр `sep` команды `print()` позволяет **установить строку**, с помощью которой будут разделены **аргументы**(если их несколько) при печати.

По умолчанию этот параметр **равен символу пробела**.

- Имея **изначальное значение параметра `sep`**, следующие строки являются **эквивалентными**:
    
    ```python
    print('aa', 'bb', 'cc')
    print('aa', 'bb', 'cc', sep=' ')
    ```
В качестве параметра `sep` **можно указывать переменные**.

#### Параметр **`end`**

Параметр **`end`** определяет, что нужно добавить в конец вывода команды **`print()`**.
Т.е. определяет **строку**, которая будет добавлена после вывода **всех аргументов** команды **`print()`**.

По умолчанию параметр **`end`** равен символу перевода строки **`\n`**

Последовательность символов **`\n`** называется **управляющей последовательностью** и задаёт перевод/перенос строки.

- Следующие строки являются **эквивалентными**:

    ```python
    print("A great man doesn't seek to lead.")
    print("He's called to it. And he answers.")
    ```

    ```python
    print("A great man doesn't seek to lead.", end='\n')
    print("He's called to it. And he answers.", end='\n')
    ```
Если перевод строки делать не нужно или требуется указать какое-нибудь специальное окончание для вывода, то следует явно указать значение для параметра **`end`**(можем указать через переменную, как и с параметром **`sep`**)

#### **Множественное присваивание**

В языке Python **за одну инструкцию присваивания** можно задавать значения сразу нескольким переменным. 

- Делается это так:

    ```python
    name, surname = 'Trofim', 'Evseev'
    print('Имя:', name, 'Фамилия:', surname)
    ```
- Этот код можно записать и так:
  
    ```python
    name = 'Trofim'
    surname = 'Evseev'
    print('Имя:', name, 'Фамилия:', surname)
    ```
Отличие двух способов состоит в том, что **множественное присваивание** в первом способе присваивает значения **двум переменным одновременно**.


#### PEP 8
При оформлении программ рекомендуется пользоваться **PEP 8 - Python Enhancement Proposal**. 

Этот документ предлагает единый и общепринятый стиль написания программ на языке Python. 

Документ создан по рекомендациям **Гвидо Ван Россума - автора Python.**

##### Некоторые рекомендации PEP 8

1. **Не** используйте пробел перед открывающей скобкой с аргументами команды **`print()`**
2. Используйте пробел после запятой **`,`** при перечислении аргументов в команде **`print()`**.
3. **Не** отделяйте пробелами оператор присваивания **'='** при определении **параметра** по умолчанию.
4. Отделяйте пробелами оператор присваивания **`=`** при определении **переменных**.

##### Комментарии

В программу имеет смысл включать **примечания, описывающие, что она делает и как она работает**. 

Это может помочь разработчику или любому другому человеку понять принцип работы программы спустя какое-то время.

Такие примечания называются **комментариями**.

Любую строку можно превратить в комментарий, поставив перед ней или после неё символ **'#'(без кавычек естественно)**.

Закоментированные строки при запуске программы игнорируются. Комментарий предназначен только для автора программы и для тех, кто будет читать код.



### Важные моменты:

**По параметрам `sep` и `end`:**

Вызов команды **`print()`** с пустыми скобками делает перевод строки.

Параметры **`sep`** и **`end`** можно устанавливать одновременно.

Для разных команд **`print()`** можно задавать **разные** параметры **`sep`** и **`end`**.

Чтобы убрать все **дополнительные выводимые символы**, можно установить параметры **`sep`** и **`end`** команды **`print()`** как **пустые строки - ('')**.

Мы не можем указывать параметры **`sep`** и **`end`** перед **аргументами**, так как **именованные параметры всегда должны следовать после позиционных аргументов**.

- Такой код:

    ```python
    print('5', '+', sep='_', '5', end='!!!', '=', '10')
    ```
     Выводит:

     ```python
     SyntaxError: positional argument follows keyword argument
     ```
Параметр **`sep`** является разделителем для нескольких аргументов в команде **`print()`**.

Если аргумент в команде **`print()`** только один, то параметру sep нечего разделять. В таком случае параметр **`sep`** никак не будет влиять на выводимый текст.

- Например приведённый ниже код:

    ```python
    print('aa', sep='777')
    ```

     Выводит:
   
     ```python
     aa
     ```

**По множественному присваиванию:**

Если слева от знака **`=`** во **множественном присваивании** должны стоять через запятую **имена переменных**, то справа могут стоять **произвольные выражения**, разделенные запятыми. 

Главное, чтобы слева и справа от знака присваивания было **одинаковое число элементов**.

**Множественное присваивание** удобно использовать, когда **нужно обменять значения двух переменных**. 

- В Python это делается так:
  
    ```python
    name1 = 'Trofim'
    name2 = 'Alibaba'
    name1, name2 = name2, name1
    ```
- Но следующий вариант для обмена значений переменных не сработает:
  
    ```python
    name1 = 'Trofim'
    name2 = 'Alibaba'
    name1 = name2
    name2 = name1
    ```
Дело в том, что инструкция **name1 = name2** полностью стирает старое значение переменной **name1**. Когда мы в инструкции **name2 = name1** присваиваем для переменной **name2** значение переменной **name1**, этим значением уже не является строка **'Trofim'**, этим значением уже является строка **'Alibaba'**.

**Новое значение переменной вытесняет старое**. Важно представлять, чему равно значение переменной в каждый момент времени.

**По комментариям:**

Согласно стандарту **PEP 8**, **комментарии** должны отделяться хотя-бы **двумя пробелами** от кода. Они должны начинаться с символа **'#'** и **одного** пробела.

### Примеры кода:

1. Замена параметра **`sep`** на символ **'*'**:

    ```python
    print('aa', 'bb', 'cc', sep='*')
    ```

     Выводит:
   
     ```python
     aa*bb*cc
     ```
2. Использование переменной в качестве параметра **`sep`**:

    ```python
    minus = '-'
    print('aa', 'bb', 'cc', sep=minus)
    ```

     Выводит:
   
     ```python
     aa-bb-cc
     ```
3. Использование переменной в качестве параметра **`end`**:
   
    ```python
    minus = '-'
    print('a', 'b', 'c', end=minus)
    print('second line')
    ```

     Выводит:
   
     ```python
     a b c-second line
     ```
4. Одновременное использование **`sep`** и **`end`**:
   
    ```python
    print('a', 'b', 'c', sep='*', end='finish')
    ```

     Выводит:
   
     ```python
     a*b*cfinish
     ```

5. Установка разных парамеров **`sep`** и **`end`** для разных команд **`print()`**:
   
    ```python
    argument_1 = 'Hello'
    sep_1 = '_-_'
    end_2 = '+++'
    print(argument_1, 'everyone', sep=sep_1, end = '! ') 
    print('How', 'are', 'you', 'in', '2026?', sep=' ', end=end_2)
    ```

     Выводит:
   
     ```python
     Hello_-_everyone! How are you in 2026?+++
     ```
6. Удаление всех дополнительных выводимых символов:
   
    ```python
    print('a', 'b', 'c', sep='', end='')
    print('d', 'e', 'f', sep='', end='')
    ```

     Выводит:
   
     ```python
     abcdef
     ```

## Конспект по теме: "Работа с целыми числами"

### Основные понятия:

Почти всё предыдущее внимание было уделено команде **`input()`**, которая по умолчанию считывает **строковые данные**. 

Но во многих случаях необходимо работать именно с **числами**.

Чтобы в **Python** создать **переменную** целого типа данных, **нужно опустить кавычки** при её объявлении(**не использовать кавычки**).

Для определения целого числа типа **`int`** используется последовательность цифр от **0** до **9**.

Явно указанное численное значение в коде программы называется **целочисленным литералом**. 

Когда **Python** встречает **целочисленный литерал**, он создаёт **объект** типа **`int`**, хранящий указанное значение.

#### Основные операции с числами:

В **Python** как в математике, над числами можно совершать **4 основные операции**.

1. Оператор **`+`** - отвечает за сложение.
2. Оператор **`-`** - отвечает за вычитание.
3. Оператор **`*`** - отвечает за умножение.
4. Оператор **`/`** - отвечает за деление.

#### Дополнительные операции с числами:

1. Оператор **`**`** - возведение в степень.
2. Оператор **`%`** -  возвращает остаток от деления.
3. Оператор **`//`** - целочисленное деление.

#### Порядок выполнения операций

В **математике** существует **порядок выполнения операций**, который определяет очерёдность и приоритетность их выполнения.

Порядок выполнения операций в **Python** аналогичен порядку выполнения операций в **математике**.

Для изменения порядка выполнения операций понадобятся **скобки**.

**Оператор возведения в степень `**`** является **правоассоциативным**(значение выражения вычисляется **справа налево**) в соответствии с правилами **математики**.

- Таким образом, выражение **`x ** y ** z`** вычисляется как **`x ** (y ** z)`**.

Операторы **`/`**, **`*`**, *`//`**, **`%`** - имеют **равные приоритеты**.

Операторы **`+`** и **`-`** также имеют **равные приоритеты** и при этом самые низкие приоритеты выполнения.

У оператора возведения в степень **`**`** **приоритет выше**, чем у **любого другого арифметического оператора**. Кроме скобок (**`()`**).

Наивысший приоритет имеют скобки - **`()`**.

#### Возведение в степень

Оператор возведения в степень **`**`** может возводить не только в **положительную степень**, но и в **отрицательную**.

В отличие от многих языков программирования, в языке **Python** для **возведения в степень** используется оператор **`**`**, а не **`^`**.

#### Целочисленное деление

Для **положительных чисел** оператор целочисленного деления **`//`** ведёт себя как **обычное деление** - **`/`**.

- Но исключением является то, что **он отбрасывает десятичную часть результата**: 

    ```python
    print(10 // 3)
    print(10 // 4)
    print(10 // 5)
    print(10 // 6)
    print(10 // 12)
    ```

     Выводит:
   
     ```python
     3
     2
     2
     1
     0
     ```

При **делении отрицательных чисел** необходимо помнить, что результат **целочисленного деления** не превосходит **частное**.

- Другими словами, **округление всегда берётся в меньшую сторону**:

    ```python
    print(10 // 3)
    print(-10 // 3)
    ```

     Выводит:
   
     ```python
     3 # округление в меньшую сторону
     -4 # округление в меньшую сторону
     ```

#### Деление с остатком

Оператор **деления с остатком** **`%`** возвращает остаток от деления **двух целых чисел**.

При нахождении **остатка от деления на целое число *n*** может получаться результат **`0, 1, 2, ...*n* - 1`**.

Например, при **делении на 2** возможны остатки 0, 1, при **делении на 3** возможны остатки 0, 1, 2 и т.д.

Оператор **деления с остатком** **`%`** очень полезен при решении огромного количества задач. Например, **число** делится на ***n*** нацело тогда и только тогда, когда остаток от **деления на *n* равен 0**.

#### Преобразование строки к целому числу

Для того, чтобы преобразовать **строку** к **целому числу**, используется команда, а точнее **функция** - **`int()`**.

Она означает следующее: "Возьми **то, что указано в качестве аргумента в скобках, и преврати это в целое число**". 

#### Преобразование целого числа к строке

Для того, чтобы **преобразовать целое число в строку** используется команда - **`str()`**

#### Обработка цифр числа

При помощи операторов **целочисленного деления `//`** и **нахождения остатка `%` от целочисленного деления** можно вычислить **любую** цифру числа.

Последняя цифра числа определяется **всегда** как **остаток от деления** числа на **`10: % 10`**.

Чтобы избавиться от **последней цифры** числа, необходимо **разделить число нацело на `10: // 10`**.

##### Алгоритм получения цифр n-значного числа

Несложно понять по какому алгоритму можно получить каждую цифру ***n*-значного** числа **`num`**:

- Последняя цифра: **`num % 10`**
- Предпоследняя цифра: **`(num // 10) % 10`**
- Предпредпоследняя цифра: **`(num // 100) % 10`**
- ....
- Третья цифра: **`(num // 10 ** (n - 3)) % 10`**
- Вторая цифра: **`(num // 10 ** (n - 2)) % 10`**
- Первая цифра: **`num // 10 ** (n - 1)`**

Формула для вычисления ***`i`* - цифры** ***n*-значного** числа **`num`** в общем виде:

**`(num // 10 ** (n - i)) % 10`**

Другой алгоритм получения каждой цифры ***n*-значного** числа **`num`**:

- Последняя цифра: **`num % 10`**
- Предпоследняя цифра: **`(num % 100) // 10`**
- Предпредпоследняя цифра: **`(num % 1000) // 100`**
- ....
- Третья цифра: **`(num % 10 ** (n - 2)) // (10 ** (n - 3))`**
- Вторая цифра: **`(num % 10 ** (n - 1)) // (10 ** (n - 2))`**
- Первая цифра: **`(num % 10 ** n) // (10 ** (n - 1))`**

Можно записать формулу для вычисления ***`i`* - цифры** ***n*-значного** числа **`num`** в общем виде:

**`num % 10 ** (n + 1 - i) // (10 ** (n - i))`**

##### Символ - разделитель

Для удобного **чтения чисел** можно использовать **символ подчёркивания** - **`_`**.

### Важные моменты:

**Числа** обозначаются **без кавычек**, а **строки — с кавычками**.

- Для того, чтобы считать одно целое число, понадобится написать следующий код:

    ```python
    number = int(input())
    ```
При работе с целочисленными переменными часто необходимо присваивать переменной целой математической формулы:

**`<имя переменной> = <формула>`**

Сокращение **`int`** происходит от английского **integer** - **целый**

В большинстве языков программирования **переменная целого типа** имеет **ограничения**.

Например, в языке C# переменная может принимать значения из диапазона [-2^63; 2^63 -1]. 

В **Python** реализована **длинная арифметика**, исходя из этого, **переменная целого типа**, по сути, **не имеет ограничений**.

**Минус** может использоваться для обозначения **отрицательных чисел**.

А вот операции **`+`**, **`/`**, **`*`** **всегда** стоят между **двумя переменными** и что-то с ними делают.

**`-`**, использующийся для обозначения **отрицательного числа** называется **унарным**.

**`-`**, использующийся для обозначения **вычитания** называется **бинарным**.

Результат операции **`/`** абсолютно **всегда** будет **числом с плавающей точкой**.

- Если при **преобразовании строки в число**, **строка** не является **целым числом**, то при преобразовании возникнет **ошибка**.
  
    ```python
    n = int('1992ABD!')
    print(n)
    ```

     Выводит:
   
     ```python
     ValueError: invalid literal for int() with base 10
     ```

**Деление** на **0** приводит к **ошибке.

### Примеры кода:

1. Преобразование **строки** к **целому числу**:
   
    ```python
    string_type = '1992'
    str_in_number = int(string_type)
    print(str_in_number)
    ```

     Выводит:
   
     ```python
     1992
     ```
2. Преобразование **строки** к **целому числу** **2 вариант**:
   
    ```python
    n = int('1992')
    print(n)
    ```

     Выводит:
   
     ```python
     1992
     ```

3. Работа оператора **деления с остатком** **`%`**:
   
    ```python
    print(10 % 3)
    print(10 % 4)
    print(10 % 5)
    print(10 % 6)
    print(10 % 12)
    print(10 % 20)
    ```

     Выводит:
   
     ```python
     1
     2
     0
     4 
     10
     10
     ```
4. Получение цифр **двузначного числа**:
   
    ```python
    num = 17
    a = num % 10   # вычисляем последнюю цифру числа
    b = num // 10  # вычисляем первую цифру числа

    print(a)
    print(b)
    ```

     Выводит:
   
     ```python
     7
     1 # То есть сначала мы вывели последнюю, а затем первую цифру числа.
     ```
5. Получение цифр **трёхзначного числа**:
   
    ```python
    num = 754
    a = num % 10          # вычисляем последнюю цифру числа
    b = (num % 100) // 10 # вычисляем среднюю цифру числа
    c = num // 100        # вычисляем первую цифру числа
    
    print(a)
    print(b)
    print(c)
    ```

     Выводит:
   
     ```python
     4
     5
     7 # То есть сначала мы вывели последнюю, затем среднюю цифру, а затем и первую цифру числа.
     ```
6. Программа для демонстрации работы всех **целочисленных операторов**:
   
    ```python
    a = 13
    b = 7

    total = a + b
    diff = a - b
    prod = a * b
    div1 = a / b
    div2 = a // b
    mod = a * b
    exp = a ** b
    
    print(a, '+', b, '=', total)
    print(a, '-', b, '=', diff)
    print(a, '*', b, '=', prod)
    print(a, '/', b, '=', div1)
    print(a, '//', b, '=', div2)
    print(a, '%', b, '=', mod)
    print(a, '**', b, '=', exp)
    ```
  
     Выводит:
   
     ```python
     13 + 7 = 20
     13 - 7 = 6
     13 * 7 = 91
     13 / 7 = 1.8571428571428572
     13 // 7 = 1
     13 % 7 = 6
     13 ** 7 = 62748517
     ```

## Конспект по теме: "Числа с плавающей точкой"

### Основные понятия:

Наравне с **целыми числами** в **Python** есть возможность работы с **дробными(вещественными) числами**.

Так, например, число **2/3** - является **вещественным** и **целого типа `int`** недостаточно для их представления.

**Дробные(вещественные) числа** в информатике называют **числами с плавающей точкой**.

Для представления **чисел с плавающей точкой** в **Python** используется тип данных **`float`**.

#### Преобразование строки к числу с плавающей точкой

- Для **преобразования строки к числу с плавающей точкой** мы используем команду **`float()`**:
  
    ```python
    str_in_num = float(input()) # преобразование считанной строки в число с плавающей точкой
    ```
- Для **преобразования строки к числу с плавающей точкой** необязательно использовать команду **`input()`**:
  
    ```python
    str_in_num = float('1.2345') # преобразование строки в число с плавающей точкой
    ```

#### Преобразование между **`int`** и **`float`**

##### Неявное преобразование

Любое **целое число**(тип **`int`**) можно использовать там, где **ожидается** **число с плавающей точкой**(тип **`float`**), поскольку **при необходимости** **Python** автоматически преобразует целые числа в числа с плавающей точкой.

##### Явное преобразование

Число с плавающей точкой **нельзя** неявно преобразовать в **целое число**.

- Для такого преобразования **необходимо использовать явное преобразование** с помощью команды **`int()`**.
  
    ```python
    num1 = 17.89
    num2 = -13.56
    num3 = int(num1)
    num4 = int(num2)
    
    print(num3)
    print(num4)
    ```

     Выводит:
   
     ```python
     17
     -13
     ```
**Преобразование чисел с плавающей точкой в целое** производится с **округлением в сторону нуля**, то есть **`int(1.7) = 1`**, **`int(-1.7) = -1`**.

### Важные моменты:

В **Python**, когда вы используете операцию деления **`/`**, результат **всегда будет числом с плавающей точкой**, даже если оба числа являются **целыми**.

В отличие от **математики**, где **разделителем является запятая**, в **информатике** **используется точка** при записи **чисел с плавающей точкой**.

## Конспект по теме: "Встроенные функции Python"

### Основные понятия:

**Python** включает множество заранее определённых **функций**. Их реализация скрыта от программиста. 

Достаточно знать, как эти **функции** называются.

1. **`print()`** - вывести на экран;
2. **`input()`** - считать с клавиатуры;
3. **`int()`** - преобразовать к целому числу;
4. **`float()`** - преобразовать к числу с плавающей точкой;
5. **`min()`** - определение минимального значения;
6. **`max()`** - определение максимального значения;
7. **`abs()`** - найти модуль числа;

#### Функции **`min()`** и **`max`**

Для определения **минимального** или **максимального значения** используются **функции `min()`**  и **`max()`**.

**Аргументов** у этих **функций** может быть сколько угодно, главное, чтобы они все поддерживали между собой **операцию сравнения**(например, **`float`** и **`int`** поддерживают **сравнение**, а **`float`** и **`str`** - **нет**).

#### Функция **`abs()`**

**Модулем**(абсолютной величиной) **положительного числа** называется само **число**, модулем **отрицательного числа** называется **противоположное ему число**, модуль **нуля** - **нуль**.

**Модуль числа** a обозначается **`∣a∣`**, для него имеет место равенство:

<img width="284" height="118" alt="image" src="https://github.com/user-attachments/assets/4f61150d-a4c3-45ae-ad18-a3f7418a8148" />

Для нахождения **модуля**(абсолютной величины) **числа** в **Python** используется функция **`abs()`**.


### Важные моменты:

**Все** три функции **`max()`**, **`min()`**, **`abs()`** - работают как с **целыми числами**, так и с **числами с плавающей точкой**.

### Примеры кода:

1. Работа **функций `min()`, `max()`**:
   
    ```python
    a = max(3, 8, -3, 12, 9)
    b = min(3, 8, -3, 12, 9)
    c = max(3.14, 2.17, 9.8) # Числа с плавающей точкой

    print(a)
    print(b)
    print(c)
    ```

     Выводит:
   
     ```python
     12
     -3
     9.8
     ```
2. Работа **функции `abs()`**:
   
    ```python
    print(abs(10))
    print(abs(-7))
    print(abs(0))
    print(abs(-17.67))
    ```

     Выводит:
   
     ```python
     10
     7
     0
     17.67
     ```
3. 
